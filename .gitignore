# Dependencies
/vendor
auth.json
# Library package — consumers resolve their own deps; don't pin a lock
composer.lock

# Caches
.cache
/build

# IDE Helper
_ide_helper.php
_ide_helper_models.php
.phpstorm.meta.php

# Editors
/.idea
/.fleet
/.vscode

# Misc
phpstan.neon
testbench.yaml
/coverage

# Internal drafts (release notes, research) — never committed
/internal

# Autoresearch artifacts (benchmarks, corpus, progress) — never committed
/autoresearch

# >>> boost (managed) >>>
# Generated by boost-core. Edit .ai/ instead.
.agents/skills/
.boost/
.claude/commands/
.claude/skills/
.config/boost/
.github/prompts/
# <<< boost (managed) <<<
