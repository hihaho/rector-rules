includes:
    - phar://phpstan.phar/conf/bleedingEdge.neon
    - vendor/spaze/phpstan-disallowed-calls/disallowed-dangerous-calls.neon
    - vendor/spaze/phpstan-disallowed-calls/disallowed-execution-calls.neon
    - vendor/spaze/phpstan-disallowed-calls/disallowed-insecure-calls.neon

parameters:
    tmpDir: .cache/phpstan

    paths:
        - src
        - tests

    level: max

    type_coverage:
        return: 100
        param: 100
        property: 100
        constant: 100
        declare: 100

    type_perfect:
        null_over_false: true
        no_mixed: false
        narrow_param: false
        narrow_return: true

    cognitive_complexity:
        class: 80
        function: 20

    strictRules:
        allRules: true

    editorUrl: 'phpstorm://open?file=%%file%%&line=%%line%%'

    treatPhpDocTypesAsCertain: false

    ignoreErrors:
        -
            identifier: trait.unused
            reportUnmatched: false
        -
            identifier: ternary.condNotBoolean
            reportUnmatched: false

        # Option::PATHS is how a rule learns which paths the consumer configured — the
        # suffix rules need it to build their rename map before traversal starts.
        # Option::REGISTERED_RECTOR_RULES is likewise the only place a registration test
        # can read back what a config file registered. Rector exposes no public accessor
        # for either.
        -
            identifier: classConstant.internal
            paths:
                - src/Rector/NamingClasses/Support/ScanCacheKeys.php
                - src/Rector/NamingClasses/Support/SuffixRenameMap.php
                - tests/Config/RenamePropagationRegistrationTest.php
                - tests/Rector/NamingClasses/RenamePropagation/AbstractRenamePropagationTestCase.php
                - tests/Rector/NamingClasses/Support/SuffixRenameMapTest.php
