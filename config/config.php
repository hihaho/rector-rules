<?php

declare(strict_types=1);

use Rector\Config\RectorConfig;

return static function (RectorConfig $rectorConfig): void {
    // No global services needed — rules are registered via sets or direct configuration.
};
